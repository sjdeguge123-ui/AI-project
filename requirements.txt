# VidGrab 依赖列表
# 安装命令: pip install -r requirements.txt

# 视频/字幕提取
yt-dlp>=2024.1.0
bilibili-api-python>=16.0.0

# AI 调用（统一接口）
litellm>=1.0.0

# 语音转录
openai>=1.0.0          # Whisper API 使用
faster-whisper>=0.10.0 # 本地 Whisper 模型

# 文档导出
python-docx>=1.0.0     # Word 导出
WeasyPrint>=60.0       # PDF 导出
Pillow>=10.0.0         # 图片导出

# 配置文件解析
PyYAML>=6.0

# MCP Server
mcp>=1.0.0
